apiVersion: v1
kind: Service
metadata:
  namespace: dev
  labels:
    name: frontend
  name: frontend
spec:
  ports:
    - protocol: "TCP"
      port: 8006
      targetPort: 4200
  selector:
    app: frontend
